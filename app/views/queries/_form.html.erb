<%= form_for(@query) do |f| %>
  <% if @query.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@query.errors.count, "error") %> prohibited this query from being saved:</h2>

      <ul>
      <% @query.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <form class="form-inline" role="form">

    <div id="form-new" class="form-group has-success has-feedback">
      <%= f.label "Ingrese la Fecha Desde" %>
      <%= f.date_field :con_fechad, :class=>"form-control", :value => Date.today  %>
    </div>

    <div id="form-new" class="form-group has-success has-feedback">
      <%= f.label "Ingrese la Fecha Hasta" %><br>
      <%= f.date_field :con_fechah, :class=>"form-control", :value => Date.today  %>
    </div>

<!--    <div class="field">
      <%= f.label :con_descripcion %><br>
      <%= f.text_area :con_descripcion %>
    </div> -->

    <div class="actions">
      <%= f.submit "Confirmar", :class => "btn btn-primary" %>
    </div><br>

  </form>
<% end %>
