<%= form_for(@rubro) do |f| %>
  <% if @rubro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rubro.errors.count, "error") %> prohibited this rubro from being saved:</h2>

      <ul>
      <% @rubro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rub_desc %><br>
    <%= f.text_field :rub_desc %>
  </div>
  <div class="field">
    <%= f.label :rub_suma %><br>
    <%= f.check_box :rub_suma %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
